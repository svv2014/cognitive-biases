/**
 * Biases that have a mechanism scene (src/lib/scenes). Kept apart from the
 * scenes themselves so a bias page can ask "is there one?" without pulling in
 * three.js.
 */
export const sceneBiasIds = ['model-collapse', 'feedback-loop-amplification', 'survivorship-bias', 'anchoring'];
